[Global]
onmissing = passthrough
audience = https://xrd3:1094, https://xrd4:1094, fts, rucio

[Issuer Keycloak]
issuer = https://keycloak:8443/realms/rucio
base_path = /data
map_subject = True
# default_user is a fallback, but 'u *' in authdb is more robust
default_user = xrootd
