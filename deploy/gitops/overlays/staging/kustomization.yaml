apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dep-dlm-staging
resources:
  - ../../base/externalsecrets        # identical to sandbox — that's the point
  - clustersecretstore.yaml           # external Vault, k8s auth (no seed job)
