apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: testbed-secrets
spec:
  refreshInterval: 1h
  secretStoreRef: { name: dep-dlm-secrets, kind: ClusterSecretStore }
  target: { name: testbed-secrets, creationPolicy: Owner }
  dataFrom:
    - extract: { key: dep-dlm-production-secrets }
