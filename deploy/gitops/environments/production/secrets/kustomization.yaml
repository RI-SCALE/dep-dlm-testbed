apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dep-dlm-production
resources:
  - ../../../base/externalsecrets        # identical — the architecture is shared
  - clustersecretstore.yaml
  - ../../../base/bootstrap         # the rucio-bootstrap-db Job (wave "5")
