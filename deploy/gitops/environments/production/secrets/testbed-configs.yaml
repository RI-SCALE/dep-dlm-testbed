apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: testbed-configs
spec:
  refreshInterval: 1h
  secretStoreRef: { name: dep-dlm-secrets, kind: ClusterSecretStore }
  target: { name: testbed-configs, creationPolicy: Owner }
  dataFrom:
    - extract: { key: dep-dlm-production-configs }
