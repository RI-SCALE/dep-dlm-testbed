apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rucio-server
spec:
  parentRefs:
    - name: dep-dlm-production-gateway
  hostnames:
    - rucio.dep-dlm-production.example.com
  rules:
    - backendRefs:
        - name: rucio-server
          port: 80
