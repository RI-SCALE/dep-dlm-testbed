apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: fts
spec:
  parentRefs:
    - name: dep-dlm-production-gateway
  hostnames:
    - fts.dep-dlm-production.example.com
  rules:
    - backendRefs:
        - name: fts
          port: 8446
