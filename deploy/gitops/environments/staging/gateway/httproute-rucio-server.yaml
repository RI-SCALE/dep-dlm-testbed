apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rucio-server
spec:
  parentRefs:
    - name: dep-dlm-staging-gateway
  hostnames:
    - rucio.dep-dlm-staging.example.com
  rules:
    - backendRefs:
        - name: rucio-server
          port: 80
