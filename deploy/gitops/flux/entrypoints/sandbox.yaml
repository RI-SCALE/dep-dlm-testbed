# ============================================================================
# DEP DLM — Flux entrypoint (sandbox)
# ============================================================================
# Ordering: namespace -> eso -> core -> secrets -> bootstrap -> components
#   core      : vault + postgresql, up BEFORE the seed so vault-seed can reach
#               Vault and ESO can project the secrets bootstrap mounts
#   secrets   : ESO ClusterSecretStore + vault-seed Job + ExternalSecrets
#   bootstrap : Rucio DB-init Job (mounts rucio-server-cfg + testbed-scripts-rucio)
#   components: rucio-server/daemons, keycloak, xrootd, teapot, fts
# Fixes: vault-seed 'no such host' (Vault now exists first) and bootstrap
# FailedMount (ESO secrets now projected before the Job runs).
---
apiVersion: v1
kind: Namespace
metadata:
  name: dep-dlm-sandbox
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata: { name: dep-dlm-sandbox-eso, namespace: flux-system }
spec:
  interval: 5m
  sourceRef: { kind: GitRepository, name: dep-dlm-testbed }
  path: ./deploy/gitops/flux/eso
  prune: true
  wait: true
  timeout: 5m
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata: { name: dep-dlm-sandbox-core, namespace: flux-system }
spec:
  interval: 5m
  sourceRef: { kind: GitRepository, name: dep-dlm-testbed }
  path: ./deploy/gitops/flux/core/sandbox
  prune: true
  wait: true                      # Vault + postgresql Ready before seed/secrets
  timeout: 10m
  dependsOn:
    - { name: dep-dlm-sandbox-eso }
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata: { name: dep-dlm-sandbox-secrets, namespace: flux-system }
spec:
  interval: 5m
  sourceRef: { kind: GitRepository, name: dep-dlm-testbed }
  path: ./deploy/gitops/environments/sandbox/secrets
  prune: true
  targetNamespace: dep-dlm-sandbox
  wait: true                      # ExternalSecrets projected before bootstrap mounts them
  timeout: 10m
  dependsOn:
    - { name: dep-dlm-sandbox-core }   # Vault must be up + reachable for the seed
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata: { name: dep-dlm-sandbox-bootstrap, namespace: flux-system }
spec:
  interval: 5m
  sourceRef: { kind: GitRepository, name: dep-dlm-testbed }
  path: ./deploy/gitops/flux/bootstrap
  prune: true
  targetNamespace: dep-dlm-sandbox
  dependsOn:
    - { name: dep-dlm-sandbox-secrets }   # rucio-server-cfg + testbed-scripts-rucio exist
  wait: true
  timeout: 10m
  healthChecks:
    - apiVersion: batch/v1
      kind: Job
      name: rucio-bootstrap-db
      namespace: dep-dlm-sandbox
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata: { name: dep-dlm-sandbox, namespace: flux-system }
spec:
  interval: 5m
  sourceRef: { kind: GitRepository, name: dep-dlm-testbed }
  path: ./deploy/gitops/flux/environments/sandbox
  prune: true
  dependsOn:
    - { name: dep-dlm-sandbox-bootstrap }
