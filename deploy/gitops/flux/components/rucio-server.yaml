apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rucio-server
  namespace: flux-system
spec:
  interval: 5m
  releaseName: rucio-server
  targetNamespace: dep-dlm-PLACEHOLDER
  install: { createNamespace: true }
  dependsOn:
    - { name: postgresql }
  chart:
    spec:
      chart: rucio-server
      version: "40.0.0"
      sourceRef: { kind: HelmRepository, name: rucio, namespace: flux-system }
  # Argo $values -> base/values/rucio-server.yaml; Flux reads it from a ConfigMap the
  # env Kustomization generates (configMapGenerator: rucio-server-values).
  valuesFrom:
    - kind: ConfigMap
      name: rucio-server-values
      valuesKey: rucio-server.yaml
