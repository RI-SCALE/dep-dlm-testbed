apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rucio-daemons
  namespace: flux-system
spec:
  interval: 5m
  releaseName: rucio-daemons
  targetNamespace: dep-dlm-PLACEHOLDER
  install: { createNamespace: true }
  dependsOn:
    - { name: rucio-server }
  chart:
    spec:
      chart: rucio-daemons
      version: "40.0.0"
      sourceRef: { kind: HelmRepository, name: rucio, namespace: flux-system }
  # Argo $values -> base/values/rucio-daemons.yaml; Flux reads it from a ConfigMap the
  # env Kustomization generates (configMapGenerator: rucio-daemons-values).
  valuesFrom:
    - kind: ConfigMap
      name: rucio-daemons-values
      valuesKey: rucio-daemons.yaml
