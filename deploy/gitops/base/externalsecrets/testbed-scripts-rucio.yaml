apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: testbed-scripts-rucio
spec:
  refreshInterval: 1h
  secretStoreRef: { name: dep-dlm-vault, kind: ClusterSecretStore }
  target: { name: testbed-scripts-rucio, creationPolicy: Owner }
  dataFrom:
    - extract: { key: dep-dlm/scripts-rucio }
