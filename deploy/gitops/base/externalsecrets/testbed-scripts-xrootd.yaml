apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: testbed-scripts-xrootd
spec:
  refreshInterval: 1h
  secretStoreRef: { name: dep-dlm-vault, kind: ClusterSecretStore }
  target: { name: testbed-scripts-xrootd, creationPolicy: Owner }
  dataFrom:
    - extract: { key: dep-dlm/scripts-xrootd }
