# Design-NNN: <title>

- **Status:** Proposed | Accepted | Superseded
- **Owner:** <team / individual>
- **Related ADR(s):** <link, if a decision this design implements has
  its own ADR — the ADR says *why*, this doc says *how*>

## Problem

<What's wrong or missing today. Concrete, with evidence — file/line
references, reproduced failures, existing duplication. This section
can and should reference actual code, unlike an ADR's context section,
because the point here is precision about the current state.>

## Goals

- <...>

## Non-goals

- <explicitly out of scope, and why — prevents scope creep and
  clarifies what a reviewer shouldn't expect this doc to resolve>

## Design

<The proposed shape: data structures, config format, function
signatures, sequence of changes. This is where implementation detail
belongs — it didn't belong in the ADR.>

## Touch points

| File / component | Change |
|---|---|
| | |

## Fallback / safety behavior

<What happens on missing config, unknown input, partial rollout —
especially important for anything with a default that must remain
safe.>

## Testing

- <unit, integration, CI jobs that confirm this design works and
  regressions are caught>

## Sequencing

1. <ordered rollout / migration steps>

## Open questions

- <unresolved items, explicitly flagged rather than silently decided>
